services:
  - type: web
    name: catalystlens-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: CATALYSTLENS_ENVIRONMENT
        value: production
      - key: CATALYSTLENS_CORS_ORIGINS
        sync: false
